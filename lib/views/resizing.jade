extends layout

block content
    mixin header('resizing')
    .rulers
        .resizerContainer.animatedDrag
            img.loading(src='/images/throbber.svg', width='48', height='48')
            .frame
                iframe.resizer.hidden(src='#{url}', height='100%', frameborder='0')
